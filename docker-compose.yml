services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: nolitour
      POSTGRES_PASSWORD: nolitour
      POSTGRES_DB: nolitour
    ports:
      - "5432:5432"
    volumes:
      - nolitour_pgdata:/var/lib/postgresql/data

volumes:
  nolitour_pgdata:
